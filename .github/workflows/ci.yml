name: CI

on:
  push:
    branches:
      - main

jobs:
  build:
    name: "Build and Test"
    runs-on: ubuntu-latest
    # strategy:
    #   matrix:
    #     node: [18, 20]

    steps:
      - uses: actions/checkout@v4

      # - name: Setup Node.js | ${{ matrix.node }}
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"

      - run: npm install
      - run: npm test

      - name: Generate Tag
        id: generate_tag
        run: |
          SHA=$(echo $GITHUB_SHA | head -c7)
          echo "sha=$SHA" >> $GITHUB_OUTPUT

      - name: Create .env File
        run: echo "DATABASE_URL=${{ secrets.DATABASE_URL }}" >> .env

      - name: Login Into The Container Registry
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build Docker Image
        uses: docker/build-push-action@v6.11.0
        with:
          context: .
          file: Dockerfile
          push: true
          tags: ericl123/rocketseat-ci-api:${{ steps.generate_tag.outputs.sha }}, ericl123/rocketseat-ci-api:latest

      # - name: Build Docker Image
      #   run: docker build -t ericl123/rocketseat-ci-api:${{ steps.generate_tag.outputs.sha }} .

      # - name: Push Docker Image
      #   run: docker push ericl123/rocketseat-ci-api:${{ steps.generate_tag.outputs.sha }}
